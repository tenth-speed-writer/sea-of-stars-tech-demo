using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Handles changes to physical forms at each tick.
/// Mostly called by the LocalPlayfieldManager.
/// </summary>
public class PhysicalFormManager : MonoBehaviour
{
    public void 
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
